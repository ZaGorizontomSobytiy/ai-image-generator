# 🎨 Генератор изображений AI

CLI приложение для генерации изображений с использованием различных AI моделей через ProxyAPI и OpenRouter.

## 📋 Описание

Проект содержит два скрипта для генерации изображений:

1. **ProxyAPI** - генерация через `gpt-image-1`
2. **OpenRouter** - генерация через `google/gemini-2.5-flash-image-preview`

Оба скрипта работают через терминал: вы вводите промпт и получаете готовое изображение в формате PNG.

## 🚀 Установка

### 1. Создание виртуального окружения

**Windows (PowerShell):**
```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

**Windows (CMD):**
```cmd
python -m venv .venv
.venv\Scripts\activate.bat
```

**Linux/Mac:**
```bash
python3 -m venv .venv
source .venv/bin/activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта и добавьте ваши API ключи:

```env
# ProxyAPI ключ
PROXY_API=ваш_ключ_proxyapi

# OpenRouter ключ
OPENROUTER_API_KEY=ваш_ключ_openrouter
```

**Где получить ключи:**
- ProxyAPI: https://proxyapi.ru/
- OpenRouter: https://openrouter.ai/

## 💻 Использование

### ProxyAPI (gpt-image-1)

Запустите скрипт:
```bash
python image_generator_proxyapi.py
```

Введите промпт, когда программа попросит:
```
Введите промпт для генерации изображения: кот-астронавт в космосе
```

Изображение будет сохранено в `generated_images/proxyapi/`

### OpenRouter (Gemini Flash)

Запустите скрипт:
```bash
python image_generator_openrouter.py
```

Введите промпт, когда программа попросит:
```
Введите промпт для генерации изображения: футуристический город в стиле киберпанк
```

Изображение будет сохранено в `generated_images/openrouter/`

## 📁 Структура проекта

```
image-generator-proxyapi/
├── .venv/                              # Виртуальное окружение
├── generated_images/                   # Сгенерированные изображения
│   ├── proxyapi/                       # Изображения из ProxyAPI
│   └── openrouter/                     # Изображения из OpenRouter
├── image_generator_proxyapi.py         # CLI для ProxyAPI
├── image_generator_openrouter.py       # CLI для OpenRouter
├── requirements.txt                    # Зависимости проекта
├── .env                                # Переменные окружения (не коммитится)
├── .gitignore                         # Исключения для Git
└── README.md                          # Документация
```

## 🔧 Технические детали

### ProxyAPI скрипт

- **Модель:** `gpt-image-1`
- **API:** https://api.proxyapi.ru/openai/v1
- **Формат вывода:** Base64 JSON
- **Размер:** Стандартный (определяется моделью)

### OpenRouter скрипт

- **Модель:** `google/gemini-2.5-flash-image-preview`
- **API:** https://openrouter.ai/api/v1
- **Формат вывода:** Data URL (base64)
- **Соотношение сторон:** 1:1 (квадратное изображение)
- **Модальности:** image, text

## 📝 Формат имён файлов

Изображения сохраняются с автоматически сгенерированными именами:

```
YYYYMMDD_HHMMSS_первые_30_символов_промпта.png
```

Пример: `20251215_151730_kot-astronavt_v_kosmose.png`

## 🛡️ Безопасность

- ✅ API ключи хранятся в `.env` файле
- ✅ `.env` добавлен в `.gitignore`
- ✅ Виртуальное окружение изолирует зависимости
- ✅ Сгенерированные изображения не коммитятся

**Важно:** Никогда не коммитьте файл `.env` с реальными ключами в публичный репозиторий!

## 🐛 Устранение неполадок

### Ошибка: "Переменная окружения не установлена"

Убедитесь, что:
1. Файл `.env` создан в корне проекта
2. В `.env` добавлены соответствующие ключи
3. Ключи не содержат лишних пробелов или кавычек

### Ошибка при активации виртуального окружения (PowerShell)

Если PowerShell блокирует выполнение скриптов:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Ошибка при генерации изображения

Проверьте:
1. Правильность API ключа
2. Наличие интернет-соединения
3. Баланс на аккаунте API сервиса

## 📦 Зависимости

- `openai` (>=1.0.0) - Клиент для работы с OpenAI-совместимыми API
- `python-dotenv` (>=1.0.0) - Загрузка переменных окружения из .env

## 🎯 Примеры промптов

**Для качественных результатов:**
- "Реалистичный портрет кота в костюме астронавта"
- "Футуристический город в стиле киберпанк, неоновые огни"
- "Пейзаж горы на закате в стиле акварели"
- "Абстрактная композиция из геометрических фигур"

**Советы:**
- Будьте конкретны в описании
- Указывайте стиль (реализм, пиксель-арт, акварель и т.д.)
- Добавляйте детали освещения и композиции
- Используйте русский или английский язык

## 📄 Лицензия

Проект создан в образовательных целях. Используйте на своё усмотрение.

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Документацию API сервисов
2. Логи ошибок в терминале
3. Правильность настройки `.env` файла

---

**Приятного использования! 🎨✨**
